[package]
name = "centinel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Centinel: a data-collection toolkit for .gov web surfaces and YouTube channels."
default-run = "centinel"

[dependencies]
centinel-core.workspace = true

anyhow.workspace = true
serde.workspace = true
serde_json.workspace = true
clap.workspace = true
tokio.workspace = true
futures.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
jiff.workspace = true
axum.workspace = true
tokio-stream.workspace = true
async-stream = "0.3"
indicatif = "0.18"
# Terminal size only. Already in the tree under indicatif, so this costs no new download.
console = "0.16"
# The `schedule set` wizard. Built on `console`, which indicatif already brought — the
# better-known `inquire` sits on crossterm, and two terminal libraries deciding
# independently about raw mode and cursor position, in a binary that also prints hours of
# progress bars, is a class of bug nobody wants.
dialoguer = { version = "0.12", default-features = false }

[dev-dependencies]
tempfile = "3"
tower = { version = "0.5", features = ["util"] }

# `setpriority` for the scheduler's threads — collection is throughput work and search is
# latency work. Already in the tree under tokio, so this costs no new download.
[target.'cfg(unix)'.dependencies]
libc = "0.2"
