---
# Vault sidecar — generated by civic-archivist. The original vault file is
# immutable; this sidecar is regenerable but its frontmatter must be preserved
# across regenerations.
sha256: "{{SHA256}}"                  # 64-char hex of the raw bytes
vault_path: "{{VAULT_PATH}}"          # e.g. pdfs/2026-04-26-a1b2c3d4-fy2025-parks-awards.pdf
sidecar_path: "{{SIDECAR_PATH}}"      # e.g. pdfs/sidecar/2026-04-26-a1b2c3d4-fy2025-parks-awards.pdf.md
source_url: "{{SOURCE_URL}}"          # the URL that first led us here (canonical)
fetched_at: "{{FETCHED_AT}}"          # ISO 8601 with timezone, e.g. 2026-04-26T14:32:11-04:00
document_kind: "{{KIND}}"             # pdf | html | excel | csv | image | transcript | docx | unknown
mime_type: "{{MIME}}"                 # from `file --mime-type`, NOT the server header
length_bytes: {{LENGTH_BYTES}}
page_count: {{PAGE_COUNT_OR_NULL}}    # PDFs only; null otherwise
extractor: "{{EXTRACTOR}}"            # web_extract | pdftotext | ocrmypdf+tesseract | trafilatura | ssconvert | soffice | whisper | tesseract | none
ocr_engine: {{OCR_ENGINE_OR_NULL}}    # e.g. "tesseract-5.3" or null
extraction_status: "{{STATUS}}"       # ok | ocr_pending | unsupported_mime | encrypted | error
seen_at:
  - url: "{{SOURCE_URL}}"
    at: "{{FETCHED_AT}}"
    discovered_via:
      investigation: "{{INVESTIGATION_OR_NULL}}"
      caller_url: "{{CALLER_URL_OR_NULL}}"
      link_text: "{{LINK_TEXT_OR_NULL}}"
      from_agent: "{{FROM_AGENT}}"    # investigator | watch-runner | cartographer | operator
paired_with: {{PAIRED_WITH_OR_EMPTY}} # e.g. screenshot vault_path for JS-rendered HTML; else []
tags: []
---

# {{TITLE}}

> Vault file: [`{{VAULT_PATH}}`](/vault/{{VAULT_PATH}}) — {{LENGTH_HUMAN}}, {{PAGE_COUNT_OR_NA}} pages
> Discovered via: {{DISCOVERED_VIA_HUMAN}}

## Source

- **URL:** {{SOURCE_URL}}
- **Fetched:** {{FETCHED_AT}} by `civic-archivist`
- **SHA256:** `{{SHA256}}`
- **MIME:** `{{MIME}}`
- **Extractor:** `{{EXTRACTOR}}`{{IF_OCR}} (OCR engine: `{{OCR_ENGINE}}`){{ENDIF_OCR}}

## Extracted text

<!--
The full plaintext extraction. For documents over ~200KB, this is truncated to
the first 50KB with a "(truncated, see vault file)" marker — the original PDF/HTML
in the vault is the source of truth, not this excerpt.
-->

{{EXTRACTED_TEXT}}

## Summary

<!--
1–3 paragraph LLM summary. Should answer:
- What is this document?
- Who created it (department, vendor, official)?
- What dates does it cover?
- What kinds of facts does it assert (claims, financial figures, names)?
- What would be needed to corroborate?
-->

{{SUMMARY}}

## Entity hints

<!--
LLM-extracted candidates only — civic-data-reporter does canonical reconciliation.
Format: bullet list, group by kind. Do not assert; suggest.
-->

**People:**
- {{...}}

**Organizations / contractors:**
- {{...}}

**Dollar amounts:**
- {{...}}

**Dates:**
- {{...}}

## Discrepancies

<!--
If during cross-check this doc's facts contradict an existing wiki/DB value, list
each here with both values + both sources. The Archivist also drops a parallel
note in <wiki>/_runtime/inbox/data-reporter/. Empty section = no conflicts found.
-->

_None detected at vault time._
