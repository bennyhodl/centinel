---
title: "{{title}}"
kind: raw
auto_published: true
watch_id: "{{watch_id}}"
generated_by: watch-runner
generated_at: "{{generated_at}}"
source_url: "{{source_url}}"
source_vault_path: "{{source_vault_path}}"   # may be null if vault not yet ready
confidence: {{confidence}}                   # 0.0–1.0; raw requires >= 0.8
severity: "{{severity}}"
hit_hash: "{{hit_hash}}"
entities: {{entities_json}}                  # JSON list of entity slugs touched
---

# {{title}}

> Auto-published from `{{watch_id}}` on {{generated_at}}. One concrete fact + citation. No editorial.

## The fact

{{one_sentence_claim}}

## Details

- **Date:** {{date}}
- **Amount:** {{amount_formatted}}
- **Parties:** {{parties}}
- **Reference:** {{reference}}

## Citation

- Source: [{{source_url_label}}]({{source_url}})
{{#if source_vault_path}}
- Vault: `{{source_vault_path}}`
{{/if}}

## Why surfaced

{{watch_reason}}

---

*Generated by `civic-watch-runner` (watch: `{{watch_id}}`). To dispute, edit `<wiki>/Watches/{{watch_id}}.yaml` or move this file to `Findings/archive/` with `status: killed` and a `kill_reason`.*
