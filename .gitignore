# Rust
/target/
**/*.rs.bk

# Centinel store — the corpus is data, not source. Rebuildable indexes and
# multi-gigabyte blob pools do not belong in git.
/.centinel/

# Local model weights, pulled by `centinel models pull`.
/models/

# env / secrets
.env
.env.local
.env.*.local

# os / editor
.DS_Store
.idea/
.vscode/
*.swp
*~
.obsidian/

# Local config (app preferences are per-machine)
centinel.toml
.claude
